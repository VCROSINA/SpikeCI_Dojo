@ -6,6 +6,9 @@ dist: trusty

jdk: oraclejdk8

env:
  - COVERAGE=0.31

install: 
  - ./gradlew clean jar

@ -17,7 +20,7 @@ after_success:
  - find . -name jacocoTestReport.csv|xargs cat|awk -F',' '{print $3" "$4" "$5} 

before_deploy:
  - ./gradlew test jacocoTestCoverageVerification
  - COVERAGE=${COVERAGE} ./gradlew test jacocoTestCoverageVerification

deploy:
  - provider: script
  
