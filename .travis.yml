@ -1,10 +1,17 @@
sudo: required

language: java
jdk: oraclejdk9

script: 
dist: trusty

jdk: oraclejdk8

install: 
  - ./gradlew clean jar

after_script:
script:
  - ./gradlew test

after_success:
  - ./gradlew jacocoTestReport
  - find . -name jacocoTestReport.csv|xargs cat|awk -F',' '{print $3" "$4" "$5}'
  - find . -name jacocoTestReport.csv|xargs cat|awk -F',' '{print $3" "$4" "$5} 
